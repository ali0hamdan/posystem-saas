/**
 * Canonical main-branch code per tenant (see multi-branch migration).
 * Default branch UUID may differ per environment; resolve via `BranchScopeService` + `clientId`.
 */
export const MAIN_BRANCH_CODE = 'MAIN';
